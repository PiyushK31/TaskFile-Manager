<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link rel="stylesheet" href="/stylesheet/style.css">
  <script src="/javascripts/script.js" defer></script>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-900 flex items-center justify-center min-h-screen">
    <div class="main w-full max-w-md bg-zinc-900 py-10 text-white">
        <a class="text-blue-500 inline-block mb-10" href="/">go back to home</a>
        <form id="taskForm" action="/edit" method="POST" class="bg-zinc-800/70 p-8 rounded-2xl shadow-xl flex flex-col gap-6">
      
            <input 
              name="Previous"
              type="text" 
              id="taskTitle"
              placeholder="Previous name" 
              class="w-full px-4 py-3 rounded-xl bg-zinc-900 text-zinc-200 placeholder-zinc-500 border border-zinc-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500"
              value="<%= filename %>"
              disabled="true"
            >
            <input type="hidden" name="previous" value="<%= filename %>">

            <input 
              name="new"
              type="text" 
              id="taskTitle"
              placeholder="new name" 
              class="w-full px-4 py-3 rounded-xl bg-zinc-900 text-zinc-200 placeholder-zinc-500 border border-zinc-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500"
            >

            <input 
              type="submit" 
              value="update name" 
              class="cursor-pointer w-full py-3 rounded-xl bg-gradient-to-r from-indigo-500 to-purple-500 text-white font-semibold shadow-md hover:shadow-lg transition-transform active:scale-95"
            >

        </form>
    </div>
</body>

</html>
